# 开发日志

> 记录每次开发的决策、改动与待办。日期为实际开发日。

---

## 2026-09-21 · v0.1.0 首个可用版本

### 需求确认（两轮问答）

| 问题 | 结论 |
|---|---|
| 学习目标 | 雅思应试 + 在美生活日常口语 |
| 核心模块 | 单词记忆（间隔重复）、计划与打卡、听读写说练习记录、错题本与语法笔记，四个全要 |
| 目标分 / 考试日期 | 还没定，做成设置项，计划按设置推算 |
| 每日时长 | 不固定，工作日和周末分别设定 |
| 数据保存 | 静态网页 + 数据同步到 GitHub 仓库 |
| AI | Claude（Anthropic），浏览器直连，用户自己提供 Key |
| 仓库 | 代码公开 `english-study`（可上 Pages）；学习数据私有 `english-study-data` |

完整需求见 [docs/需求文档.md](docs/需求文档.md)。

### 架构决策

- **纯静态、无构建**：HTML + CSS + 原生 JS，`<script>` 顺序加载，`file://` 直接打开也能用。理由：个人项目，长期维护成本最低；手机浏览器也能直接用。
- **数据与密钥分离**：学习数据存 `es_data_v1`，API Key / GitHub Token 存 `es_secrets_v1`，导出与同步只碰前者。这样 `data.json` 可以放心提交到私有仓库，公开仓库永远不会带密钥。
- **同步用 GitHub Contents API**：一个 Fine-grained Token 只授权私有数据仓库的 Contents 读写。推送前先 GET 拿最新 `sha`，若远程 `updatedAt` 比上次同步时间新（别的设备改过），弹窗让用户选覆盖方向。数据变动后延迟 4 秒自动推送，避免频繁提交。
- **AI 浏览器直连 Claude**：使用 `anthropic-dangerous-direct-browser-access: true` 头直接 fetch `/v1/messages`，不需要中转服务器。默认模型 `claude-opus-5`；结构化输出（单词补全）用 `output_config.format = json_schema`；简单任务 `effort: low`，批改用 `medium`。启用了 `fallbacks: "default"`（beta 头 `server-side-fallback-2026-07-01`），安全分类器拒答时服务端自动换模型重试；`stop_reason === "refusal"` 时给出明确提示。
- **间隔重复**：SM-2 变体，四档评分。新词首轮间隔 1/2/4 天，第二轮 3/6/10 天，之后按 ease 倍增；“忘了”10 分钟后重现并回到本轮队尾。每日新词配额 `dailyNewWords`，按 `learnedOn` 计数。
- **每日任务生成**：总时长按工作日/周末取值；25% 单词、15% 美国生活口语（口语专项日除外）、其余给当日专项；≥90 分钟时再切 35% 给副项。专项按星期轮换，可在设置中改。

### 本次完成

- [x] 项目骨架、样式（响应式，深色模式跟随系统，手机端底部导航）
- [x] 数据层、密钥分离、JSON 导入导出、清空
- [x] 单词：复习（翻卡、四档评分、快捷键、朗读）、词库（搜索/标签/编辑/删除/CSV 导出）、单个添加与批量导入、AI 补全
- [x] 计划：设置项、每日任务、打卡、连续天数、周柱状图、历史
- [x] 练习：四类记录 CRUD、详情展开、写作/口语 AI 反馈、AI 生成练习材料并可存为记录
- [x] 笔记：错题/语法两类，标签与搜索，AI 讲解
- [x] 设置：计划参数、GitHub 同步（测试连接/推送/拉取/自动同步）、Claude Key 与模型、数据管理
- [x] 浏览器实测：总览、单词复习评分、计划打卡、练习新增、笔记新增、设置页均正常，控制台无报错
- [x] README、需求文档、开发日志
- [x] 建 GitHub 仓库并推送，开启 Pages
  - 代码（公开）：<https://github.com/chuchu1214w/english-study>
  - 数据（私有）：<https://github.com/chuchu1214w/english-study-data>
  - 在线地址：<https://chuchu1214w.github.io/english-study/>

### 已知限制 / 待办

- [ ] AI 功能尚未用真实 Key 端到端验证（需要用户填入 Key 后点“测试调用”）
- [ ] 同步冲突只做“整体覆盖”，没有字段级合并；两台设备同时改会丢一边
- [ ] 口语练习靠手动粘贴文字稿，没有内置录音转文字（可用手机输入法语音转文字代替）
- [ ] 没有内置词库；后续可加“雅思核心词一键导入”
- [ ] 数据量大后 `data.json` 单文件同步会变慢（估计上万词才会明显）
- [ ] 复习统计图（每日复习量、记忆保持率）
- [ ] PWA 离线缓存与主屏图标

### 下一步建议

1. 用真实 Key 跑一遍 AI 补词、批改、讲解，按实际输出微调提示词
2. 用一周，看每日任务的时长拆分是否合理，再调比例
3. 加“今日复习报告”和记忆曲线图
